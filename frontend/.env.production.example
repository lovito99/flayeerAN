# Vacio usa el mismo dominio: /api y /uploads pasan por el proxy web.
# Usa https://api.tu-dominio.com si el backend vive en otro dominio.
VITE_API_URL=

# Cambia a /subcarpeta/ si publicas el frontend bajo una ruta.
VITE_BASE_PATH=/
